import { API_URL } from '../_config';

const API = {
  USERS: `${API_URL}/users`,
  SEARCH: `${API_URL}/search`,
  BOOKMARKS: `${API_URL}/bookmarks`,
  HOME: `${API_URL}/home`,
  CATEGORY: `${API_URL}/category`,
  NEWS_DETAIL: `${API_URL}/news_detail`,
  PROFILE: `${API_URL}/profile`
};

export default API;
